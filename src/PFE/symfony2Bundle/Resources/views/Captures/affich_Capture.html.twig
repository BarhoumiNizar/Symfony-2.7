{%extends 'applicationBundle::starter.html.twig'%}

{%block body%}
{%block content%}

<div class="box box-info">
    <div class="box-header">


        <h3 class="box-title">Liste des Captures</h3>




    </div>
    <div class="pagination">
        <span class="disabled"><< prev</span><span class="current">1</span><a href="">2</a><a href="">3</a><a href="">4</a><a href="">5</a>…<a href="">next >></a>
    </div> 
    <div class="box-footer clearfix">
        <div class="col-lg-12">
            <h1 class="page-header"></h1>
            <div>  
                <table id="rounded-corner" summary="2007 Major IT Companies' Profit">
                    <thead>


                        <tr><th scope="col" class="rounded">id capture</th>
                            <th scope="col" class="rounded">Capteur</th>
                            <th scope="col" class="rounded">Date</th>

                            <th scope="col" class="rounded">Mesure</th>
                            <th scope="col" class="rounded">Valeur</th>

                            
                            <th scope="col" class="rounded">Delete</th>
                        </tr>
                    </thead>

	{%for line in res%}
                    <tbody>
                        <tr>    <td>{{line.id}}</td>
                            <td>{{line.capteur.nomCapteur}}</td>

                            <td>{{line.dateCapture|date('Y-m-d')}}</td>


                            <td>{{line.nomMesure}}</td>
                            <td>{{line.valeurMesure}}</td>

                          
                            <td><a href="{{path('delete_Capture',{'id':line.id})}}" onclick="if (!confirm('voulez-vous supprimer ce capture'))
                    return false" class="ask"><img src="{{asset('bundles/application/images/trash.png')}}" alt="" title="" border="0" /></a></td>
                        </tr>           

      {%endfor%}
                    </tbody>
                </table>


                <a href="{{path('application_form_Ajout_Capture')}}" class="bt_red"><span class="bt_red_lft"></span><strong>ADD NEW</strong><span class="bt_red_r"></span></a> 



{%endblock%}
{%endblock%}


